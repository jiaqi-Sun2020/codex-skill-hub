interface:
  display_name: "LaTeX Paper Pipeline"
  short_description: "Build LaTeX paper pipelines with evidence-aware figure routing."
  default_prompt: "Use $latex-paper-build-skill to create or restructure a research-paper pipeline with .bib-managed references, one manuscript figures/ root, optional routing through $academic-figure-workflow, and body text that interprets every included figure without confusing visual QA with scientific claim approval."
