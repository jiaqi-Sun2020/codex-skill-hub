interface:
  display_name: "Research Project Pipeline"
  short_description: "Orchestrate one-time setup with separate readiness axes"
  default_prompt: "Use $research-project-pipeline once to plan, initialize, and verify a research project workspace; keep readiness axes separate, audit the final submission surface when applicable, then prepare a compact handoff and give ongoing information maintenance to Neat-Freak."
