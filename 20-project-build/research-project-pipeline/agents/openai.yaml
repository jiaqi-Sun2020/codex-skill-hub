interface:
  display_name: "Research Project Pipeline"
  short_description: "Orchestrate one-time setup with separate readiness axes"
  default_prompt: "Use $research-project-pipeline once to plan, initialize, and verify a research project workspace; keep onboarding, domain validation, execution authorization, and claim support separate, then hand ongoing information maintenance to Neat-Freak."
