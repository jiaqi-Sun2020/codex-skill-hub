interface:
  display_name: "Research Project Pipeline"
  short_description: "Orchestrate one-time setup and safe legacy bootstrap completion"
  default_prompt: "Use $research-project-pipeline once to discover, plan, initialize or safely complete a legacy project's missing startup bootstrap, and verify the workspace; keep knowledge, bootstrap, governance, execution, and claim readiness separate, then hand ongoing information maintenance to Neat-Freak."
