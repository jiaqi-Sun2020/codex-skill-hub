interface:
  display_name: "Project Agent Generator"
  short_description: "Generate initial .agents context and startup loading"
  default_prompt: "Use $project-agent-generator-skill once to generate this project's initial .agents context, nested memory baseline, and project-local Codex startup bootstrap."
